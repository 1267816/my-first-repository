# C 语言学习 · 交接文档

> 更新于 2026-09-26。开新对话时，把这个文件路径发给新对话，让它先读这份再继续。
> 本文件放在 GitHub 仓库 `my-first-repository` 根目录，具体代码在 `C-Learning/` 下。

## 1. 一句话背景

暨南大学计算机科学与技术大一新生，军训已结束，**2026-09-28（周一）正式上课**，
第一门编程课大概率是 C 语言。零基础起步（高中信息技术课学的 Python 已忘光，
只跟过 3 小时 B 站前端入门），靠 B 站浙大翁恺《C 语言程序设计》入门课自学，
目标是开学后能跟上甚至领先课程进度。

## 2. 当前进度（截至 2026-09-26）

- 翁恺入门课学到**第 5 周**（第 1–4 周：程序入门、计算、判断、循环，已过）。
- 第 4 周（循环）四道作业 `04-0` ~ `04-3` 已全部写完并提交，实测**全部通过课程样例**：

| 题号 | 题目 | 验证结果 |
| --- | --- | --- |
| 04-0 | 求符合给定条件的整数集 | 输入 2，输出与样例逐行一致 |
| 04-1 | N 位水仙花数 | 输入 3 输出 153/370/371/407；输入 7（上限）输出 1741725/4210818/9800817/9926315，耗时约 0.4 秒 |
| 04-2 | 打印九九口诀表 | 输入 4，`%-4d` 左对齐格式与样例一致 |
| 04-3 | 统计素数并求和 | 输入 `10 31` 得 `7 143`；边界 `1 10` 得 `4 17`；`1 500` 得 `95 21536` |

- 第 5 周刚开始：只写了 `C-Learning/week5/video/factor.c`（阶乘，for 循环），
  `week5/practice/answer`、`week5/practice/homework` 两个文件夹还是空的 —— **下一步就填这两个**。

## 3. 代码放哪

| 项目 | 位置 |
| --- | --- |
| 本地仓库 | `D:\Github_Repo\my-first-repository` |
| 远程仓库 | <https://github.com/1267816/my-first-repository>（`git@github.com:1267816/my-first-repository.git`） |
| C 语言代码 | `C-Learning/week1..week5/`，每周下分 `video/`（跟课代码）、`practice/answer`、`practice/homework` |
| 前端练习 | `front-end-practice/`（暑假学的那点前端） |
| Markdown 笔记 | `Markdown-learning/` |
| 可复用代码模块 | `C-Learning/week4/module/用while循环遍历n位正整数.c`（自己拆出来的，习惯很好） |

Git 用 GitHub Desktop 管理，写完一批就提交并推送，保持 `main` 与 `origin/main` 同步。
编译产物（`.exe`）由 `.gitignore` 排除，不入库。

## 4. 环境（已全部装好，不需要重装）

| 项目 | 位置 / 说明 |
| --- | --- |
| Dev-C++ 6.3（主环境，跟翁恺视频用） | `C:\Users\25878\Dev-Cpp`，桌面有快捷方式 |
| gcc 9.2 + gdb（TDM-GCC） | `C:\Users\25878\Dev-Cpp\TDM-GCC-64\bin`，已加入用户 PATH |
| VS Code（备用环境） | 已装 C/C++ 扩展，Ctrl+Shift+B 编译运行、F5 调试 |
| Git / GitHub Desktop | 提交与推送 |

## 5. 已解决过的问题（新对话不要再重复踩坑）

1. **Dev-C++ 中文只在光标所在行显示**：编辑器字体被设成了 Consolas（无中文字形），
   已改为 **NSimSun（新宋体）**。**不要改回 Consolas。**
2. **中文输出乱码**：UTF-8 源码编译后 Windows 控制台按 GBK 显示。
   已在 Dev-C++ 编译参数和 VS Code 的 `tasks.json` 里加 `-fexec-charset=GBK`。
3. **`height.c` 算错**：`scanf` 格式串里的逗号与提示语的空格不匹配，第二个数读不进去。
   教训：**`scanf` 的格式串必须和实际输入逐字符对应**。
4. **改 `devcpp.ini` 前必须完全关闭 Dev-C++**，否则退出时会用内存里的设置覆盖配置文件。

## 6. 编码约定（最重要的一条）

- 所有 `.c` 文件**统一用 UTF-8 保存**，不要在 Dev-C++ 里另存为 ANSI/GBK。
- 中文 `printf` 现在控制台显示正常，无需再处理。
- 编译产物（`.exe`）不进 Git。

## 7. 下次可以顺手改进的地方（不着急，等学到对应知识再回头改）

- `04-3.c` 素数统计：现在的写法是"枚举所有乘积对、数到 1 个因数就判为素数"，
  **结果是对的**（含 `1 500` 边界），但每个数都要把 1..N 跑一遍。
  学完第 5 周的 `break` / 标志位后，可以重写成"试除到平方根，找到因数就 break"。
- `04-0.c` 里四层嵌套 `if (a!=b) { if (a!=c) ... }`，第 3 周学的 `&&` 可以直接串成一行。
- `factor.c`（阶乘）有个真实坑：`int` 存不下 13!，`n=13` 输出 1932053504、`n=20` 变负数。
  第 6 周"数据类型"会讲整数范围，届时用 `long long` + `%lld` 解决。
- Git 提交信息可以更具体：把 `Create some files` 这类改成 `week4: 完成水仙花数 04-1`，
  以后回看历史更容易。

## 8. 开学后的节奏

- 课程共 14 周，一周跟一周，期末前正好学完入门 126 集。每周 5–7 小时
  （工作日每天半小时 + 周末 2 小时），比突击更稳。
- 按周推进：第 6 周数据类型 → 第 7 周函数 → 第 8 周数组 → 第 9 周指针（**不要跳指针周**）
  → 第 10 周字符串 → 第 11 周结构 → 第 12 周程序结构 → 第 13 周文件 → 第 14 周链表。
- 第一堂课确认教材和课程用的语言；若第一门课是 Python，主线换小甲鱼视频，路线不变。
- 继续保持"排错日记"和每天打卡的习惯。
- 原始学习计划：`C:\Users\25878\Documents\Codex\2026-08-14\9-8-b-3-html-css\outputs\C语言100小时预习计划.md`
  （时间表按 8/14–9/7 写的，日期已过期，节奏以本文档第 8 节为准）。

## 9. 另一条线：英语泛读（不在本仓库）

用《The Week Junior》53 期做泛读，目标 2027-06-12 第一次四级笔试前读完。
资料、计划表和重排脚本都在 `C:\Users\25878\Desktop\the week junior\`，
细节见那个文件夹里的 `README.md`。

## 10. 常用资源

| 用途 | 链接 |
| --- | --- |
| 翁恺 C 入门（126 集，主线） | <https://www.bilibili.com/video/BV1yentzxEqK/> |
| 翁恺 C 进阶（57 集，入门后看） | <https://www.bilibili.com/video/BV1Hs411A7NS/> |
| 郝斌 C 语言（备选/查漏补缺） | <https://www.bilibili.com/video/BV1XLPveWE48/> |
| 数据结构（开学后）王卓 | <https://www.bilibili.com/video/BV1nJ411V7bd> |
| 小甲鱼 Python（若第一门课是 Python） | <https://www.bilibili.com/video/BV1Ef421B7dR/> |
| 洛谷刷题 | <https://www.luogu.com.cn/> |

## 11. 新对话怎么接

开新对话时直接说：

> 先读 `D:\Github_Repo\my-first-repository\交接文档.md`，然后我们继续学 C 语言。

然后把你卡住的题、报错信息或想学的知识点发过来即可。
